# threejmain Project Context

Every future AI agent, developer, or maintainer should read this file before changing the project. Update it whenever architecture, deployment, features, commands, branches, workflows, or decisions change.

## 1. Project Overview
`threejmain` is a separate project from `3JCentralPisowifi`.

## 2. Location
Project directory:

```text
/home/threejmain
```

## 3. Separation From 3JCentralPisowifi
This project must remain independent from the existing `3JCentralPisowifi` project.

Existing separate project:

```text
/home/threejpisowifi
/opt/3jcentralpisowifi-staging
```

Do not reuse `3JCentralPisowifi` ports, Docker Compose project names, databases, volumes, or environment files unless explicitly requested.

## 4. Current Status
Initial empty project scaffold created.

## 5. Decisions To Define
- Application purpose
- Tech stack
- Deployment method
- Git repository
- Environment names
- Ports
- Database/storage requirements

